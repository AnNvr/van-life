

// Script to populate the Firestore
/* const populateDatabase = async () => {
    try {
        await Promise.all(users.map(async (user) => {
            await addDoc(usersCollection, user)
        }))
    } catch (error) {
        console.log("Error populating database:", error)
    }
}

populateDatabase() */